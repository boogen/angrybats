using UnityEngine;
using System.Collections;

public class shfhuufff : MonoBehaviour {

	// Use this for initialization
	void Start () {
		Rigidbody ghd = GetComponent<Rigidbody>();
		ghd.AddForce(new Vector3(-200, 500, 0));
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
